var simbol = '*';
var space = ' ';
var line = '';
var count = 8;

for (let index = 0; index < count; index++) {
    line += space;
    console.log(line + simbol);
}

console.log("---------------------")

var line = '';

for (let index = 0; index < count; index++) {
    line += simbol;
    console.log(line);
}

console.log("---------------------")

var line = '';

for (let index_space = count - 1; index_space <= 0; index_space--) {
    line_space += 
    line += simbol;
    console.log(line);
}

